import React from 'react'

export default class GifList extend React.Component {

  handlePropsMap = () => {
    this.props.data.
  }

  render(){
    return(
      <ul>
        {this.props.data}.map()
    )
  }
}
